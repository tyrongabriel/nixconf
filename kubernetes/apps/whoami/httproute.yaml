apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: whoami-route
  namespace: whoami
spec:
  parentRefs:
    - name: shared-gateway
      namespace: kube-system
  hostnames:
    - "whoami.tyrongabriel.com"
  rules:
    - backendRefs:
        - name: whoami
          port: 80
