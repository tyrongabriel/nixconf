apiVersion: gateway.networking.k8s.io/v1beta1
kind: ReferenceGrant
metadata:
  name: allow-whoami-to-authentik
  namespace: authentik
spec:
  from:
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      namespace: whoami
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      namespace: it-tools
  to:
    - group: ""
      kind: Service
      name: ak-outpost-authentik-embedded-outpost
