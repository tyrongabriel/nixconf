apiVersion: v1
kind: Secret
metadata:
  name: authentik-secret
  namespace: authentik
type: Opaque
stringData:
  # Encrypt this file with SOPS before committing!
  # Generate a secret key with: openssl rand -base64 60
  AUTHENTIK_SECRET_KEY: "CHANGE_ME_GENERATE_WITH_openssl_rand_base64_60"
